/*
import React from "react";

import { ComponentStory, ComponentMeta } from "@storybook/react";
import { AddForm } from "../components/AddForm";
import { action } from "@storybook/addon-actions";

export default {
  title: "AddForm",
  component: AddForm,
  argTypes: {
    variant: {
      options: ["primary", "secondary"],
      control: { type: "select" },
    },
  },
} as ComponentMeta<typeof AddForm>;

const addTaskCallback = action("Add new task");

export const AddItemFormExample: ComponentStory<typeof AddForm> = () => (
  <AddForm addTaskCallback={addTaskCallback} placeholder="Add task" />
);
*/
export {}
